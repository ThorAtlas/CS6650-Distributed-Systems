package Server.Paxos;

import java.util.ArrayList;
import java.util.List;

public class PaxosServerImpl {

  public static void main(String[] args){
    //myserver1 port 2222
    //myserver2 port 3333
    //pass these into a string
    //timeout(5s)
    //start all the servers on the registries
    List<PaxosServer> listOfServer = new ArrayList<>();
    PaxosServer server = new PaxosServer();
    for ()
    listOfServer.add(server);

    //Main function have several arguments:
    //first argurment be the port number to assign it to
    //second argument host name
    //the rest will be paired of names and ports of other servers
    //pass those


  }

}
